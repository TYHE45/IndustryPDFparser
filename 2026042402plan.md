# 2026042402plan — 管道质量 P1（safety-net 压降 + 数值参数黑名单 + OCR 对齐护栏）

> 本 plan 承接 2026042401plan 第一批收口，聚焦 P1 三阶段（A：safety-net 按场景拆桶 + prompt 收紧；B：parser 数值参数黑名单升级；C：OCR 表格对齐白盒护栏），目标是 4 个独立 commit 一次性推到 `origin/main`。

---

# Context

管道质量 P1 三阶段计划（原 `first-principles-md-nested-crab` plan）已经开始推进。当前实际进度：

- **阶段 A.1 已就地完成（未提交）**：`src/text_localization.py` 的全局 safety-net 计数器已从 `int` 改为按场景分桶的 `dict`，内部 `kind` 保持 `display / source / condition / tag` 英文字面量，外部落盘时映射为中文场景键 `显示 / 来源 / 条件 / 标签`；新增 `get_safety_net_trigger_detail()`；`get_safety_net_trigger_count()` 继续返回总数（=字典值之和）
- **FP §11 已就地追加（未提交）**：新增 "2026042401plan 阶段 0–5 第一批整体收口" 说明，以及 "P1 — 整体流程再审视（2026042401plan 收口后浮出的结构层改进）" 6 条结构层待办

**当前未提交的 working tree 内容：**

```
 M First Principles.md            +34 行（2026042401plan 收口说明 + 整体流程再审视 6 条）
 M src/text_localization.py       +26 / -6（计数器从 int 改 dict）
```

这两个改动互相独立：FP 是 plan 收口后的全局文档同步，`text_localization.py` 是阶段 A.1 的具体代码。它们**都属于本轮 P1 的开场**，放在同一个"奠基" commit 里一起推出去是合理的。

**本次规划目的：** 给 A.2 → 最终 push 的全部剩余步骤一份可照做的实施清单。不重新论证已有决策，只把"下一步执行"落到具体文件 / 接口 / 断言 / 提交边界。

---

# Plan

## 阶段 A：safety-net 按场景拆桶 → prompt 收紧 → 实测压降

### A.2：`src/pipeline.py` 写入 `安全网触发明细`

- 当前 L18 `from src.text_localization import get_safety_net_trigger_count, reset_safety_net_trigger_count`
  → 改为同时导入 `get_safety_net_trigger_detail`
- 当前 L239 `"安全网触发次数": get_safety_net_trigger_count(),`
  → 在其后追加一行 `"安全网触发明细": get_safety_net_trigger_detail(),`
  → **不**改 `"安全网触发次数"` 现有字段，保持旧契约
- 不改 reset / get 调用位置（L34 `reset_safety_net_trigger_count()` 保留不动）

### A.3：`tests/test_pipeline_safety_net_count.py` 扩展断言

在现有 `test_process_log_includes_safety_net_trigger_count` 之后新增一条：
- `test_process_log_includes_safety_net_trigger_detail`
- 触发现有样例（一次 `localize_display_text("1 Scope", ...)` + 一次 `localize_tag_text("Material and design")`）
- 断言：
  - `process_log["安全网触发明细"]` 是 `dict`
  - 键集合等于 `{"显示", "来源", "条件", "标签"}`
  - `显示` ≥ 1、`标签` ≥ 1
  - `sum(process_log["安全网触发明细"].values()) == process_log["安全网触发次数"]`

原测试保留，继续断言总数等于 2。

### A.Commit 1 — "feat: safety-net 触发按场景拆计数 + FP 整体流程再审视"

- 文件：`src/text_localization.py` / `src/pipeline.py` / `tests/test_pipeline_safety_net_count.py` / `First Principles.md`
- 预检：`python -m unittest discover -s tests -p "test_*.py"` 仍 27 → 28 全通过（+1 条新断言）
- 提交信息要点：
  - A.1 改了计数器结构（保留旧 API）
  - A.2 把明细字段落到 `process_log.json`
  - A.3 把断言 +1
  - FP §11 本轮整体流程再审视 6 条结构层待办一并入库，方便后续归因

### A.4：slow baseline 采样当前分布（判断 prompt 收紧重点）

- `SLOW_TESTS=1 python -m unittest tests.test_sample_score_baseline 2>&1 | tee _tmp_baseline_pre.log`
- 对应 12 份样例的 `process_log.json` 路径约为 `_tmp_review_output/<name>/process_log.json`
- 用一条临时 Python 汇总（不入库）把每份的 `安全网触发明细` 累加到四类总和表
- 同时记录总分（收紧前基线），后续回归用
- 把结果汇总写进下一轮 commit 的提交消息里（不必落盘 JSON）

### A.5：基于 A.4 分布收紧 prompt

命中最高的场景决定改法。**实施时只改输出最多的 top 1–2 场景**，避免一次改太广：

- 若 `显示` 最高：`src/summarizer.py` system prompt（L61-74 附近）**追加 2–3 条 few-shot**，形如：
  - 示例：输入章节标题 "1 Scope"，期望输出 "适用范围"
  - 示例：输入章节标题 "Material and design"，期望输出 "材料与结构"
  - 明确禁止把章节标题以 "（原文：…）" 括注形式保留在中文摘要里
- 若 `标签` 最高：`src/tagger.py` system prompt（L75-85 附近）同样加 few-shot：
  - 示例：输入主题 "working pressure"，期望输出 "工作压力"（单中文短语，不带原文）
  - 示例：输入主题 "Ambient temperature"，期望输出 "环境温度"
- **不动 temperature、不动 response_format、不引入新依赖**

### A.6：压降验证

- `SLOW_TESTS=1 python -m unittest tests.test_sample_score_baseline 2>&1 | tee _tmp_baseline_post.log`
- 用同一段 Python 统计新的四类总和
- 判定：
  - **触发总数下降 ≥30%** ✅ → 保留 prompt 改动进入 commit 2
  - **触发下降但 <30%** → 可接受，照常进 commit 2，只是 FP 记实测数字，不强行拉高门槛
  - **分数任何一份超出 ±3 容差** → 回滚 prompt 改动；A.5 失败不影响 A.1–A.3 的成果
- 如果 slow baseline 因 LLM 抖动本身就在容差外飘，记录但不作判定依据

### A.Commit 2 — "chore: prompt few-shot 收紧 + safety-net slow baseline 压降对照"

- 文件：`src/summarizer.py` 或 `src/tagger.py`（视 A.4 结果）+ `First Principles.md`（追加前/后数字对照）
- FP §11 "2026-04-24" 块追加 1 条：
  - "safety-net 压降实测：12 份 slow baseline 触发次数 N → M（-X%）"
  - 附按场景分布：`显示 a→a' / 来源 b→b' / 条件 c→c' / 标签 d→d'`
- 若 A.6 判定为 "回滚 prompt"，这个 commit 退化为 "docs: FP 记录 safety-net 分布基线"，只带 FP 改动

---

## 阶段 B：parser 数值参数黑名单 + STANDARD_RE 年号后缀测试

### B.1：`src/parser.py` 黑名单升级到 contains

- 新增辅助 `_contains_banned_substring(text: str) -> bool`（放在现有 `_should_reject_parameter_candidate` 附近）：
  - 子串匹配 `DATE_LIKE_RE`（`\d{4}[-—/.]\d{1,2}...`）
  - 子串匹配 `METADATA_PARAM_RE`（发布 / 实施 / ICS / 分类号 等）
  - 子串匹配 `STANDARD_RE`（整体标准号）
- 在 `_should_reject_parameter_candidate`（L1347）里针对**名称字段**（不是 value 字段）新增一层 `if _contains_banned_substring(名称): return True`
  - value 字段保留 fullmatch 逻辑，避免误伤"6.0 Ω⋅m"这类合法数值

### B.2：`tests/test_parser_numeric_blacklist.py` 新建 3 条

- `test_date_in_descriptive_name_rejected`：
  - 输入参数 `{名称: "发布 2020-01", 原始值: "..."}` → `_should_reject_parameter_candidate` 返回 True
- `test_year_suffix_from_standard_code_rejected`：
  - 输入参数 `{名称: "2020", 原始值: "..."}` （模拟从 "CH/T 1234-2020" 剥离）→ 返回 True
- `test_standard_code_with_year_suffix_matched`：
  - `STANDARD_RE.fullmatch("GB 39038-2020")` 非 None
  - `STANDARD_RE.fullmatch("CH/T 1234-2020")` 非 None

### B.3：slow baseline 刷新（按需）

- 本阶段改动预期对 `GB 39038-2020` 扫描样例有感：若它从 63 分回升到 > 65 分，更新 `tests/test_sample_score_baseline.py` 对应 baseline 并在 FP §11 追加一行对照
- 若所有样例纹丝不动 → B 阶段纯预防回归，不改 baseline

### B.Commit 3 — "feat: 数值参数黑名单升级为 contains + STANDARD_RE 年号测试"

- 文件：`src/parser.py` + `tests/test_parser_numeric_blacklist.py`（+ 可能的 `tests/test_sample_score_baseline.py` + `First Principles.md`）
- 预检：`python -m unittest discover -s tests -p "test_*.py"` 全通过（28 → 31）

---

## 阶段 C：OCR 表格单元格对齐 white-box 护栏

### C.1：`tests/test_ocr_whitebox.py` +1 条

- 新增 `test_build_table_matrix_gracefully_handles_alignment_miss`：
  - 构造一组 cell_boxes（2 行 2 列）+ 故意放一组完全超出边界的 ocr_lines
  - 调 `_build_table_matrix_from_cells(cell_boxes, ocr_lines)`
  - 断言：
    - 返回是 2×2 矩阵（行列数与 cell_boxes 匹配）
    - 所有单元格是 `""` 空串，不是 `None` 或异常
    - 不抛 `IndexError / KeyError / AttributeError`

### C.Commit 4 — "test: OCR 单元格对齐失败护栏"

- 文件：`tests/test_ocr_whitebox.py`
- 预检：`python -m unittest discover -s tests -p "test_*.py"` 全通过（31 → 32）

---

## 最终：推送与收口

- `git log origin/main..HEAD --oneline` 应为 4 条（commit 1–4）
- `git push origin main`
- 本地 `git status --short` 干净

---

# Critical Files

| 路径 | 本轮作用 |
|------|---------|
| `src/text_localization.py` | **A.1 已完成**（未提交）：计数器从 int 改 dict；新增 `get_safety_net_trigger_detail` |
| `src/pipeline.py` | A.2：L18 import 扩展；L239 后增 `"安全网触发明细"` 字段 |
| `tests/test_pipeline_safety_net_count.py` | A.3：加一条 per-kind 断言 |
| `src/summarizer.py` | A.5：system prompt 加 2–3 条 few-shot（若 A.4 显示 `显示` 场景最高） |
| `src/tagger.py` | A.5：system prompt 加 2–3 条 few-shot（若 A.4 显示 `标签` 场景最高） |
| `src/parser.py` | B.1：新增 `_contains_banned_substring`；在 `_should_reject_parameter_candidate` 名称字段层应用 |
| `tests/test_parser_numeric_blacklist.py` | B.2：新建 3 条测试 |
| `tests/test_ocr_whitebox.py` | C.1：+1 条对齐失败护栏 |
| `tests/test_sample_score_baseline.py` | B.3：按需更新 baseline 数字 |
| `First Principles.md` | **已就地追加**（未提交）：2026042401plan 收口 + 整体流程再审视；Commit 2 再追加 safety-net 压降前/后数字 |

---

# Verification

**阶段 A（Commit 1 前）：**
- `python -m unittest discover -s tests -p "test_*.py"` 27 → 28 全通过
- 新断言验证 `安全网触发明细` 字段结构 + 值之和 = 总数

**阶段 A（Commit 2 前）：**
- `SLOW_TESTS=1 python -m unittest tests.test_sample_score_baseline` 前/后日志都保留
- safety-net 总数下降数字写入 FP §11
- 12 份样例分数全部在 ±3 容差内

**阶段 B（Commit 3 前）：**
- 28 → 31 全通过
- 若 baseline 更新，对照行已写入 FP §11

**阶段 C（Commit 4 前）：**
- 31 → 32 全通过
- 对齐失败路径断言"空串而非异常"成立

**全部完成后：**
- `git log origin/main..HEAD --oneline` = 4 条
- `git push origin main` 成功，`git status --short` 干净

---

# 风险与回滚点

- **A.5 prompt 收紧可能触发 LLM 本身输出抖动**：若 12 份样例里出现 >1 份样例分数飘出 ±3，回滚 prompt，保留 A.1–A.3 的计数拆分
- **B.1 contains 升级可能误伤合法参数名**：提交前手跑 2 份真实样例的 `数值型参数.json`，抽检 10 行看是否漏挡合法项
- **slow baseline 全量耗时**：A.4 与 A.6 两次跑 slow baseline 会占较长时间；若时间受限，可先跑对 LLM prompt 改动最敏感的 4–5 份样例（SN775 英文 / SN544-1 中文 / Dixon 产品目录 / GB 39038 扫描 / CB_Z 281 选型），作为 A.4 的 proxy 分布，再在 A.6 跑完整 12 份做最终判定

---

# 当前进度快照（2026-04-24 实际落地情况）

> 本段是 plan 落地过程中的实时进度记录，而非 plan 本身的一部分。

- **Commit 1 已落地**（`6ed8793 feat: safety-net 触发按场景拆计数 + FP 整体流程再审视`）：
  - A.1 / A.2 / A.3 三阶段同 commit 推出，`python -m unittest discover` 27 → 28 全通过
  - 工作树干净
- **A.4 proxy 采样进行中**：`_tmp_safety_net_sampler.py --proxy` 在跑 5 份代表性样例
  - 已完成 SN775_2009-07_e：分布 `{显示: 28, 来源: 30, 条件: 26, 标签: 13}`，总计 97 次，总分 88.0（基线 88 ±3 内）
  - 剩余 4 份：Dixon.2017 / SN544-1 / GB 39038-2020 / CB_Z 281-2011
  - 临时脚本与产物都落在 `_tmp_safety_net_baseline_pre/`，受 `.gitignore: _tmp_*/` 保护，不入库
